# Archives des digests

Un fichier par jour, `YYYY-MM-DD.md`, écrit par l'agent à la fin de son run : le message envoyé
plus les URLs retenues. Sert de mémoire anti-doublons — l'agent relit les 7 derniers avant de
sélectionner ses items.

Ces fichiers n'existent que sur la branche `state`.
